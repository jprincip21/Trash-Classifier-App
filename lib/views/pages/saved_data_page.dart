import 'package:flutter/material.dart';

class SavedDataPage extends StatelessWidget {
  const SavedDataPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Saved Data"));
  }
}
